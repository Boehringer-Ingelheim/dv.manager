% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter.R
\name{get_filtered_dataset_list_}
\alias{get_filtered_dataset_list_}
\title{Get Filtered Data}
\usage{
get_filtered_dataset_list_(
  unfiltered_dataset_list,
  filter_info,
  dataset_names,
  dataset_vars,
  dataset_extra_masks
)
}
\arguments{
\item{unfiltered_dataset_list}{A named list of \code{data.frame} objects representing the original,
unfiltered datasets.}

\item{filter_info}{A named list produced by the filtering machinery. Must contain the element
\code{result$filter_info}, where each entry is keyed by dataset name and holds:
\describe{
\item{\code{mask}}{A logical vector with one element per row of the corresponding dataset.}
\item{\code{lvls}}{A named list mapping factor-variable names to the levels that should be
retained after filtering.}
}}

\item{dataset_names}{A character vector of dataset names to process. Must be a subset of
\code{names(unfiltered_dataset_list)}. Defaults to all datasets in \code{unfiltered_dataset_list}.}

\item{dataset_vars}{A named list of character vectors specifying which columns to keep for each
dataset. Names must be a subset of \code{names(unfiltered_dataset_list)}. Defaults to all columns
of each dataset.}

\item{dataset_extra_masks}{A named list of additional logical vectors (one per dataset) that are
AND-combined with the mask from \code{filter_info}. Defaults to an empty list (no extra masking).}
}
\value{
A named list of filtered \code{data.frame} objects, one per entry in \code{dataset_names}, with
rows, columns, factor levels, and variable labels all reconciled against the original data.
}
\description{
Function that applies filter masks and column selections to a named list of
datasets. For each requested dataset it: applies the pre-computed boolean mask from
\code{filter_info}, optionally combines it with caller-supplied extra masks, restricts columns to
those listed in \code{dataset_vars}, restores factor levels according to the filter's level
information, and copies variable labels back onto the result.
}
\keyword{internal}

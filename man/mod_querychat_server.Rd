% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_querychat.R
\name{mod_querychat_server}
\alias{mod_querychat_server}
\title{Chat-based (natural language) population filter server}
\usage{
mod_querychat_server(
  id,
  selected_dataset_list,
  subject_filter_dataset_name,
  filter_key_var,
  querychat_args = list()
)
}
\arguments{
\item{id}{module id, use \code{ID$QUERYCHAT}}

\item{selected_dataset_list}{reactive returning the currently active dataset_list
(same reactive fed to \code{new_filter_server()}/\code{mod_subgroup_server()} in \code{app_server.R})}

\item{subject_filter_dataset_name}{string, the same \code{filter_dataset_name} used for the
regular subject/population filter (checked already in \code{check_filter_dataset_name()});
registered as the PRIMARY \code{querychat} table.}

\item{filter_key_var}{string, the same \code{filter_key} used everywhere else (checked in
\code{check_filter_key()})}

\item{querychat_args}{a named list forwarded to \code{querychat::QueryChat$new()} (e.g.
\code{client}, \code{greeting}, \code{data_description}, \code{extra_instructions}, \code{categorical_threshold}).
If \code{querychat_args$data_description} is not supplied, one is generated via
\code{build_querychat_data_description()}.}
}
\value{
A reactive function with the SAME calling contract as the reactive returned by
\code{mod_subgroup_server()} / \code{new_filter_server()}'s consumers: calling it returns
\verb{list(result = list(subjects = <character vector or NULL>), error_list = <error_list>)}.
\code{subjects = NULL} means "chat filter not active / no constraint" (must not restrict rows).
}
\description{
Chat-based (natural language) population filter server
}
\keyword{internal}

# Get a single label from a dataframe

It will try to read the label attribute from one column.

## Usage

``` r
get_lbl(df, var)
```

## Arguments

- df:

  a dataframe.

- var:

  from which column do we want the label

## Value

A string with the label name
